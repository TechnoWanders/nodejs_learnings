// MODULES 
/*const john = 'john';
const peter = 'peter';

const sayHi = (name) => {
    console.log(`hello there ${name}`); // Use backticks for template literals
};

sayHi('susan');
sayHi(john);
sayHi(peter);
*/

const pane = require('./basic0')
console.log(pane.jason);
console.log(pane.veronica);
console.log(pane.items);

